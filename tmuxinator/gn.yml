# ~/.tmuxinator/gn.yml

name: gn
root: ~/Sites/web
startup_pane: 1

windows:
  - API:
      layout: even-horizontal
      panes:
        - cd gn-api && bundle exec rails server -p 3000
        - cd gn-accounts && bundle exec rails server -p 3001
  - Client: cd gn-web && yarn start
